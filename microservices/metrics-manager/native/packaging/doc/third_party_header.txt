Third-Party Programs
====================

This file lists the third-party software distributed in, or linked into, the
Metrics Manager bare-metal package. It is generated by native/build.sh from the
pins in versions.env and from the module graph reported by the Go toolchain, so
it cannot drift from what is actually shipped.

The full licence texts are available at the project URLs given below.

Two binaries are redistributed unmodified:
