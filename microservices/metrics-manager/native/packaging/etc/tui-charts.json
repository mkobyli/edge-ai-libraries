{
  "SPDX-FileCopyrightText": "(C) 2026 Intel Corporation",
  "SPDX-License-Identifier": "Apache-2.0",
  "historyDuration": "5m",
  "maxPoints": 600,
  "chartHeight": 6,
  "details": {
    "defaultMetric": "cpu.totalPercent",
    "chartHeight": 10,
    "mouse": true,
    "metrics": [
      {"metric": "cpu.totalPercent", "title": "CPU utilization", "unit": "%", "min": 0, "max": 100},
      {"metric": "cpu.classPercent", "title": "Core class utilization", "unit": "%", "min": 0, "max": 100},
      {"metric": "cpu.temperatureC", "title": "CPU temperature", "unit": "°C", "min": 0, "max": 110},
      {"metric": "cpu.frequencyMHz", "title": "CPU frequency", "unit": "MHz"},
      {"metric": "cpu.powerW", "title": "CPU package power", "unit": "W"},
      {"metric": "memory.usedPercent", "title": "Memory used", "unit": "%", "min": 0, "max": 100},
      {"metric": "memory.bandwidthMiBps", "title": "Memory bandwidth", "unit": "MiB/s"},
      {"metric": "gpu.utilizationPercent", "title": "GPU utilization", "unit": "%", "min": 0, "max": 100},
      {"metric": "gpu.enginePercent", "title": "GPU engine utilization", "unit": "%", "min": 0, "max": 100},
      {"metric": "gpu.temperatureC", "title": "GPU temperature", "unit": "°C", "min": 0, "max": 110},
      {"metric": "gpu.frequencyMHz", "title": "GPU tile frequency", "unit": "MHz"},
      {"metric": "gpu.powerW", "title": "GPU graphics power", "unit": "W"},
      {"metric": "gpu.vramPercent", "title": "GPU VRAM used", "unit": "%", "min": 0, "max": 100},
      {"metric": "npu.utilizationPercent", "title": "NPU utilization", "unit": "%", "min": 0, "max": 100},
      {"metric": "npu.temperatureC", "title": "NPU temperature", "unit": "°C", "min": 0, "max": 110},
      {"metric": "npu.frequencyMHz", "title": "NPU frequency", "unit": "MHz"},
      {"metric": "npu.powerW", "title": "NPU power", "unit": "W"},
      {"metric": "npu.memoryMiB", "title": "NPU memory", "unit": "MiB"}
    ]
  },
  "charts": [
    {
      "metric": "cpu.totalPercent",
      "title": "CPU utilization",
      "unit": "%",
      "min": 0,
      "max": 100
    },
    {
      "metric": "memory.usedPercent",
      "title": "Memory used",
      "unit": "%",
      "min": 0,
      "max": 100
    },
    {
      "metric": "cpu.temperatureC",
      "title": "CPU temperature",
      "unit": "°C",
      "min": 0,
      "max": 110
    },
    {
      "metric": "gpu.utilizationPercent",
      "title": "GPU utilization",
      "unit": "%",
      "min": 0,
      "max": 100
    },
    {
      "metric": "gpu.temperatureC",
      "title": "GPU temperature",
      "unit": "°C",
      "min": 0,
      "max": 110
    },
    {
      "metric": "npu.utilizationPercent",
      "title": "NPU utilization",
      "unit": "%",
      "min": 0,
      "max": 100
    }
  ]
}
